python3 -m sglang.bench_serving --backend sglang-oai-chat --dataset-name image --num-prompts ${NUM_PROMPT} --apply-chat-template --random-output-len ${OUT_LEN} --random-input-len ${IN_LEN} --image-resolution ${IMAGE_SIZE} --image-format jpeg --image-count ${IMAGE_COUNT} --image-content blank --random-range-ratio 1 --max-concurrency ${MAX_CONCURRENCY} --host=127.0.0.1 --port=${PORT}
